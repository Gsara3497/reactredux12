## **Day 32 Task**

## **React Redux Task**

[https://drive.google.com/file/d/1fOadeM1liwbUK38z92F0XYugk2jwqK2r/view?usp=share_link]

-.From the above link get a JSON data and create a cart page with per-item price, per-item quantity,
Total quantity and total amount

**Functionalities Required**

Needs to Increase or Decrease the per unit Quantity that should automatically updates the total Quantity and Amount.

## **Procedure:-**

Step 1: Open command prompt then write cd desktop -> [npm -v] ->[npm create vite@latest] next project name[reactredux12] -> select [react] -> [javascript]

Step 2: Open file manager then select project name [reactredux12] tpye cmd then open command prompt type (code .) then create vs code.

Step 3: To create React Redux Task to the task.

step 4: Then open terminal -> To installation of using terminal for npm install redux, npm install @reduxjs/toolkit, npm install react-redux then next npm run dev to create web page for the result.

Step 5: Then we can see React Redux Task for github and netlify.com
